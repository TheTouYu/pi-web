"use strict";

module.exports = Object.freeze({
  PI_VERSION: "0.82.1",
  PI_WEB_VERSION: "0.8.2",
  COMPANION_VERSION: "1.0.0",
  HUB_PROTOCOL_VERSION: 2,
  MAX_FRAME_BYTES: 16 * 1024 * 1024,
  MAX_BROWSER_QUEUE_BYTES: 4 * 1024 * 1024,
  RECONNECT_MAX_MS: 30_000,
  DISCONNECT_RESERVATION_MS: 2 * 60_000,
  COMMAND_DEDUPE_ENTRIES: 256,
  COMMAND_DEDUPE_MS: 15 * 60_000,
  INVALIDATION_COALESCE_MS: 50,
});
